package springproject.jobschedule.controller;

public class InventoryController {
    // Manages HTTP requests for inventory operations, such as adding new items, updating stock levels, and querying inventory status.
}
