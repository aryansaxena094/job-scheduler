package springproject.jobschedule.controller;

public class OrderController {
    // Handles HTTP requests related to order operations. For example, it includes methods to create an order, update an order, fetch all orders, and delete an order.
}
