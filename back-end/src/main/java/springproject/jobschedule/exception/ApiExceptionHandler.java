package springproject.jobschedule.exception;

public class ApiExceptionHandler {
    // Handles exceptions across the application, ensuring that clean and consistent error responses are sent to the client.
}
