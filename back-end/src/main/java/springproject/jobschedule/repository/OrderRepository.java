package springproject.jobschedule.repository;

public class OrderRepository {
    // Interface for database operations related to orders, using Spring Data MongoDB to abstract the database access. It might include custom queries besides the standard CRUD operations.
}
